auditBayesianOptionalStopping <- function(jaspResults, dataset, options, ...) {
  # For the Bayesian optional stopping constituents, see auditCommonFunctions.R
  .jfaEvaluationStage(options, jaspResults, workflow = FALSE)
  
}
